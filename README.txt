В командной строке выбрать эту папку с помощью:

(cd и рассположение файла), у меня это выглядит так:

cd "C:\Users\Админ\Desktop\organizer"

затем еще 2 команды:

pip install flask flask-sqlalchemy flask-login werkzeug

python app.py

потом сайт появится по ссылке:

http://127.0.0.1:5000/emergency